# Manifests

Track logical IDs, repository paths, target/version metadata, source location, size, hashes, extraction/generation method, verification state, deduplication relationships, and known differences. Unknown values remain explicit.