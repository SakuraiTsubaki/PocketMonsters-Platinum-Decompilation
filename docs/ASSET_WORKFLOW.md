# Asset Workflow

Identify exact source → deterministic extraction/reconstruction → preserve native identity/hashes → add reviewable representation when useful → convert only for a defined target → deduplicate by byte/hash evidence → register manifest → round-trip validate → publish in small batches. Cross-generation output keeps both source identity and target contract.