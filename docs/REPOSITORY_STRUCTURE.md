# Repository Structure

Create stable purpose-based areas only when they contain real work: `docs/`, `manifests/`, `src/`, `data/`, `assets/`, `tools/`, `tests/`, `patches/`. Platform-specific verified needs may extend this locally. Do not create repository-wide structure-version roots, migration-history trees, or duplicate canonical homes.